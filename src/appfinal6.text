import "../node_modules/bootstrap/dist/css/bootstrap.min.css";
import Calculations from "./Calculations";
import { createContext, useState, useEffect } from "react";
import Te from "./Context/Te";
import Modal from "./Model";

import { createElement } from "react";

import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faEye, faEyeSlash,faUser,faPhone,faEnvelope, faHashtag,faList, faLock } from "@fortawesome/free-solid-svg-icons";
import video from "./hvid.f147a847.webm";
import loginlogo from "./loginLogo.png";
import "./components/registr.css";
import "./Project1.css";
import photoName from "./alaa.jpeg";
import audioError from "./sound/error.mp3";
import audioSuccess from "./sound/success.mp3";
import audioWarning from "./sound/warning.mp3";
import Modal2 from "./Model2";
import axios from "axios";
import MyVerticallyCenteredModal from "./modalLog";

export const UseData = createContext(false);
function App() {
  // const [formInputs, setFormInput] = useState({
  //   inputName: "",
  //   inputPassword: "",
  // });

  // const [showModdal, setShowModal] = useState(false);
  // const [errorMassge, setErrorMassge] = useState(null);
  const [showPassword, setShowPassword] = useState(false);
    const [errorMessage, setErrorMassge] = useState(null);
   const [showModal, setShowModal] = useState(false);
    const [check, setCheck] = useState(false);

  // const btn = document.querySelectorAll(".btnRipples");
  // btn.forEach((btn) => {
  //   btn.onClick = function (e) {
  //     let x = e.clientx - e.target.offsetLeft;
  //     let y = e.clientY - e.target.offsetTop;

  //     let ripplse = createElement("span");
  //     ripplse.style.left = x + "px";
  //     ripplse.style.top = y + "px";
  //     this.appendChild(ripplse);

  //     setTimeout(() => {
  //       ripplse.remove();
  //     }, 1000);
  //   };
  // });

 
  // if(window.location.reload){
  //     setTimeout(() => {
  //       window.location.href =window.location.pathname

  //      }, 1500);
     
  // }


// انشاء حساب
 const[register,setRegister]=useState(false)
  const [formData, setFormData] = useState({ name: "", email: "", password: "" });
  const [message, setMessage] = useState(null);
  const[disabledAdd, setDisabledAdd]=useState(false)

    // ضع روابط أو مسارات الأصوات الخاصة بك هنا
  const audioSuccess = "/sounds/success.mp3";
  const audioError = "/sounds/error.mp3";

  const audio1 = new Audio(audioSuccess);
  const audio3 = new Audio(audioError);


 const handleChange = (e) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

       
  const handleRegister = async (e) => {
    e.preventDefault();
    setMessage(null);
    setErrorMassge(null);
    setShowModal(false);


    const { name, email, password } = formData;

    if (!name || !email || !password) {
      setMessage("❌ جميع الحقول مطلوبة");
      setErrorMassge("❌  جميع الحقول مطلوبة");
      setShowModal(true);
      
      
      return;
    }







    try {
      const res = await axios.post("http://localhost:8082/register", formData);
      setMessage("✅ تم إنشاء المستخدم بنجاح");
      setErrorMassge("✅ تم إنشاء المستخدم بنجاح");
      setShowModal(true);
    } catch (err) {
      if (err.response && err.response.status === 409) {
        setMessage("❌ الاسم أو البريد الإلكتروني مستخدم بالفعل");
        setErrorMassge("❌ الاسم أو البريد الإلكتروني مستخدم بالفعل");
      setShowModal(true);
      } else {
        setMessage("❌ حدث خطأ أثناء إنشاء المستخدم");
       setErrorMassge("❌ حدث خطأ أثناء إنشاء المستخدم");
      setShowModal(true);
      }
    }
  };



//نهايه انشاء حساب

  const [formInputs, setFormInputs] = useState({ inputName: "", inputPassword: "" });
  const [userData, setUserData] = useState(null);

 
 



  const handelInputChange = (e) => {
    const { name, value } = e.target;
    setFormInputs(prev => ({ ...prev, [name]: value }));
  };





  const handelCheckDisable = async (e) => {
    e.preventDefault();
    setErrorMassge(null);
    setShowModal(false);

    const { inputName, inputPassword } = formInputs;


    

    if (!inputName || !inputPassword) {
      setErrorMassge("❌ الرجاء تعبئة الاسم وكلمة السر");
      setShowModal(true);
      audio3.play();
      return;
    }


    if (inputName === "alaa" && inputPassword === "12345") {
  const fakeUser = {
    id: 0,
    name: "alaa",
    email: "offline@example.com",
    offline: true,
  };

  setUserData(fakeUser);

  // 🟡 نحفظ بيانات المستخدم في localStorage
  localStorage.setItem("name", fakeUser.name);
  localStorage.setItem("email", fakeUser.email);
  localStorage.setItem("offline", "true"); // ← لتعرف لاحقًا أنه دخل بدون اتصال

  setErrorMassge("🚨 تم الدخول بدون الاتصال بالسيرفر");
  setCheck(true);
  setShowModal(false);
  audio1.play();

  window.location.href = "https://alaaahmed2024.github.io/alaa/#/start";
  return;
}




    try {
      const result = await axios.post("http://localhost:8082/login", {
        name: inputName,
        password: inputPassword,
      });

      const user = result.data;
      setUserData(user);
      localStorage.setItem("name", user.name);
      console.log(user.offline)
      
      if (user.offline) {
       setErrorMassge("🚨 تم الدخول في وضع غير متصل بقاعدة البيانات ");

      }else{
       setErrorMassge(null);
      }
      setCheck(true);
      setShowModal(false);

      audio1.play();

      // بعد نجاح الدخول، توجه للرابط المطلوب
      window.location.href = "https://alaaahmed2024.github.io/alaa/#/start";

    } catch (err) {
      // في حالة الخطأ من السيرفر (مثل 401)
      setCheck(false);
      setErrorMassge("❌ اسم المستخدم أو كلمة السر غير صحيحة");
      setShowModal(true);
      audio3.play();
    }

    
  };














// const [userData, setUserData] = useState(null);
// // جلب بيانات المستخدم من السيرفر
// const fetchData = async () => {
//   try {

// const result = await axios.post("http://localhost:8082/login", {
//   name: formInputs.inputName,
//   password: formInputs.inputPassword
// });


  
//     setUserData(result.data);
//     localStorage.setItem("name", result.data.name);
//   } catch (err) {
//     console.log("⚠️ فشل الاتصال - استخدام بيانات افتراضية");
//     const fallbackUser = { name: "alaa", password: "123456" };
//     setUserData(fallbackUser);
//     localStorage.setItem("name", fallbackUser.name);
//   }
// };




// ✅ تنفيذ عند تحميل الصفحة
// useEffect(() => {
//   fetchData();
// }, []);






// const fetchData = async () => {
//   try {
//     const result = await axios.post("http://localhost:8082/login", {
//       name: "name",       // ضع هنا القيم القادمة من نموذج أو ثابتة حسب حالتك
//       password: "password"
//     });

//     // ✅ تم الوصول للمستخدم من قاعدة البيانات
//     setUserData(result.data);

//     // مثال: تخزين الاسم
//     localStorage.setItem("name", result.data.name);

//   } catch (err) {
//     console.log("⚠️ فشل الاتصال بقاعدة البيانات، استخدام القيم الافتراضية");

//     // ⚠️ استخدام بيانات افتراضية
//     const fallbackUser = {
//       name: "alaa",
//       password: "123456"
//     };

//     setUserData(fallbackUser);

//     // ممكن أيضًا تخزينها محليًا
//     localStorage.setItem("name", fallbackUser.name);
//   }
// };



//   const [userData, setUSerData] = useState([]);
//   useEffect(() => {
//     fetchData();
//   }, []);

//   const fetchData = async () => {
//     try {
//       const result = await axios("http://localhost:8082/login");
//       //console.log(result.data);
//       setUSerData(result.data[1]);

//       //  if(result.status===200){
//       //   window.localStorage.setItem('name' , name  )

//       //   window.localStorage.getItem('name')?
//       //   window.localStorage.removeItem('name')
//       //  }
// console.log(result)
// console.log(userData)

//     } catch (err) {
//       console.log("somthing Wrong DataBase");
//     }
//   };

  

  
//   if (userData.name != undefined || userData.password != undefined) {
//     var name = userData.name;
//     var pass = userData.password;
//   } else {
//     var name = "alaa";
//     var pass = "123456";
//   }













  // const [data, setData] = useState([]);

  // useEffect(() => {
  //   fetch("http://localhost:8082/login")
  //     .then((res) => res.json())
  //     .then((data) => setData(data))
  //     .catch((err) => console.log(err));
  // });

  // if(sx.name==="alaa"){
  //   var cc=sx.name + sx.password
  // }else{
  //   var cc="no"
  // }

  // if(sx.name==="alaa"){
  //   var cc1=sx.name + sx.password
  // }else{
  //   var cc1="no"

  // }
  // console.log(cc)
  // console.log(cc1)

  // const [check, setCheck] = useState(false);

  if (check) {
    var photpUser = photoName;
  } else {
    var photpUser = faUser;
  }


//   function handelCheckDisable(event) {
//   event.preventDefault();
//   let audio1 = new Audio(audioSuccess);
//   let audio3 = new Audio(audioError);

//   setErrorMassge(null);

//   const { inputName, inputPassword } = formInputs;

//   // ✅ تحقق من أن البيانات موجودة
//   if (!userData) {
//     setErrorMassge("⚠️ لم يتم تحميل بيانات المستخدم بعد");
//     audio3.play();
//     return;
//   }

//   // ✅ استخراج الاسم وكلمة السر من userData
//   const name = userData.name;
//   const pass = userData.password;

//   if (inputName === name && inputPassword === pass) {
//     // ✅ تسجيل دخول ناجح
//     setCheck(true);
//     setErrorMassge(""); // مسح الرسائل السابقة
//     localStorage.setItem("name", name);

//     console.log("تم تسجيل الدخول:", name);
//     audio1.play();
//     window.location.href = "https://alaaahmed2024.github.io/alaa/#/start";
//   } else {
//     // ❌ فشل التحقق - إظهار الرسالة المناسبة
//     if (inputName !== name && inputPassword !== pass) {
//       setErrorMassge("❌ خطأ: تأكد من الاسم وكلمة السر");
//     } else if (inputPassword !== pass) {
//       setErrorMassge("❌ خطأ: تأكد من كلمة السر");
//     } else if (inputName !== name) {
//       setErrorMassge("❌ خطأ: تأكد من الاسم");
//     }

//     audio3.play();
//     setCheck(false);
//     setShowModal(true);
//   }
// }


//   function handelCheckDisable(event) {
//     let audio1 = new Audio(audioSuccess);
//     let audio2 = new Audio(audioWarning);
//     let audio3 = new Audio(audioError);

//     event.preventDefault();
//     setErrorMassge(null);

//     const { inputName, inputPassword } = formInputs;

//   //   if (inputPassword != pass && inputName != name) {
//   //     setErrorMassge(" خطأ تاكد من الاسم و الرقم السري");
//   //     audio3.play();
//   //   } else if (inputPassword != pass) {
//   //     setErrorMassge(" خطأ تاكد من رقم السري");
//   //     audio3.play();
//   //   } else if (inputName != name) {
//   //     setErrorMassge(" خطأ تاكد من الاسم");
//   //     audio3.play();
//   //   }
//   //   setShowModal(true);




//   //    if (inputName === name && inputPassword === pass) {
//   //     setCheck(true);
//   //      window.localStorage.setItem('name' , name  )
      
//   //     var log =window.localStorage.getItem('name')
//   //     console.log(log)
//   //     console.log(name)
//   //     // حذفها

//   //     window.location.href = "https://alaaahmed2024.github.io/alaa/#/start";
//   //     audio1.play();

    

//   //   } else {
//   //     setCheck(false);
//   //   }
//   // }

//   if (!userData) {
//   setErrorMassge("⚠️ لم يتم تحميل بيانات المستخدم بعد");
//   return;
// }

// const name = userData.name;
// const pass = userData.password;

//   if (inputName === name && inputPassword === pass) {
//   // ✅ تسجيل دخول ناجح
//   setCheck(true);
//   setErrorMassge(""); // مسح الرسائل السابقة
//   window.localStorage.setItem("name", name);

//   console.log("تم تسجيل الدخول:", name);
//   audio1.play();
//   window.location.href = "https://alaaahmed2024.github.io/alaa/#/start";
// } else {
//   // ❌ فشل التحقق - إظهار الرسالة المناسبة
//   if (inputName !== name && inputPassword !== pass) {
//     setErrorMassge("❌ خطأ: تأكد من الاسم وكلمة السر");
//   } else if (inputPassword !== pass) {
//     setErrorMassge("❌ خطأ: تأكد من كلمة السر");
//   } else if (inputName !== name) {
//     setErrorMassge("❌ خطأ: تأكد من الاسم");
//   }

//   audio3.play();
//   setCheck(false);
//   setShowModal(true);
// }
//   }


//========================

//الخروج بعد 10 دقايق من اخر حركه
var frequency = null
var loggedout = false
 
window.onblur = function(){ if(!loggedout)enableTimeout() }
window.onfocus = function(){ if(frequency!==null){clearTimeout(frequency);frequency=null} }
window.onclick = function(){ if(frequency!==null){clearTimeout(frequency);frequency=null} }
 
function enableTimeout(){
  frequency = setTimeout(function(){
    loggedout = true
    localStorage.clear()
   
  
    setShowModal(true)
    let audio2 = new Audio(audioWarning);
    audio2.play();
    setErrorMassge("جاري تسجيل الخروج ");

    setTimeout(() => {
   // window.location='https://alaaahmed2024.github.io/alaa/#/'
    window.location.replace('https://alaaahmed2024.github.io/alaa/#/')
    window.location.reload()
    console.log("User logged out")
    }, 2300);
  
 


  },(10*60*1000)) // 10 minutes timrout
}

//======================



//الخروج بعد نص ساعه حتي لو بتتحرك


 var hours = 30/60; 
var now = new Date().getTime();
var setupTime = localStorage.getItem('setupTime');
if (setupTime == null) {
    localStorage.setItem('setupTime', now)
} else {
    if(now-setupTime > hours*60*60*1000) {
        localStorage.clear()
        localStorage.setItem('setupTime', now);
    }
}


 
  
  const btnIsDisable =
    formInputs.inputName == "" || formInputs.inputPassword == "";

  function handelDivClick() {
    if (showModal == true) {
      setShowModal(false);
    }
  }

  const [forgot, setForget] = useState(false);
 
  const [sendEmail, setSendEmail] = useState("");
  const btnIsDisableSendEmail = sendEmail == "";

  const [diaplay, setDisplay] = useState("display-none");

  function getRandomNumber(min, max) {
    var xx = Math.floor(Math.random() * (max - min + 1)) + min;
    return xx;
  }

  const hangleSendEmail = () => {
    setDisplay("p-red");
  };

  if(register===true){
    return(
   





      







      <div style={{ marginTop: "0px" }} id="timeout">
        <video
          width="300px"
          height="300px"
          className="vedio"
          autoplay="autoplay"
          muted
          loop
          src={video}
        >
          Your browser does not support the video tag.
        </video>
        <div className="video-overlay"></div>
        <div className="form-div ">
          <div className="form-login ">
            <div className="card css-login">

          {showModal?(<>
          
            <>
    

      <MyVerticallyCenteredModal
        show={showModal}
        errorMassage={errorMessage}
        onHide={() => setShowModal(false)}
        dialogClassName="modal-90w"
      />
    </>
          
          </>):(<></>)}



{/* 
                <Modal2
                  style={{
                    width: "100px !important",
                    height: "100px !important",
                    background: "#ffafaf",
                  }}
                  isVisble={showModal}
                  errorMassage={errorMessage}
                /> */}




              <div className="ineer-container">
                <div className="form-logo">
                  <div className="form-logo-ineer ">
                    <img
                      src={loginlogo}
                      style={{ width: "70%" }}
                      alt="logo form"
                    />
                  </div>
                </div>
                <div div className="form-logo">
                  <div className="ineer-container">
                    <div className="css-text">
                      <label className="css-login-lable ">
                        رجاء تسجيل الحساب
                      </label>
                    </div>
                  </div>
                </div>

                <div className="form-logo">
                  <div className="input-form-login">
                    <form
                      className="flex-dir"
                      id="loan-form-start"
                      onSubmit={handleRegister}
                    >
                      <div className="circle" style={{ zIndex: "-1" }}>
                        <FontAwesomeIcon icon={faUser} className="photo-user" />
                      </div>
                      <hr style={{ margin:" 15px 5px 5px 5px" }}></hr>








                        <label htmlFor="name">Name</label>
                        <div className="flex-password">
                          <div className="icon-password">
                            <FontAwesomeIcon icon={faUser} />
                          </div>

                          <div style={{ width: "100%", margin: "6px" }} className="input-wrapper-2">
                            <input
                              style={{ margin: "0", height: "32px" }}
                              id="name"
                              className="form-control"
                              
                              autoFocus
                              placeholder="اسم المستخدم"
                                name="name"
        
                           value={formData.name}
                           onChange={handleChange}
       
                            />
                                                                                                    <span  className="underline-input-2"></span>
                   
                   
              
                          </div>
                        </div>












                        <label htmlFor="password">password</label>
                        <div className="flex-password">
                          <div className="icon-password">
                            <FontAwesomeIcon icon={faLock} />
                          </div>
                          <div style={{ width: "100%" }} className="input-wrapper-2">
                            <input
                              style={{
                                margin: "6px",
                                width: "95%",
                                height: "32px",
                              }}
                              className="form-control"
                              type={showPassword ? "text" : "password"}
                              autoComplete="true"
                              
                              placeholder="الرقم السري"
                              id="password"
        
                        name="password"
        
                        value={formData.password}
                        onChange={handleChange}



                            />
                           <span  className="underline-input-3"></span>
                          </div>

                          <div
                            className="icon-password-show"
                            onClick={() => setShowPassword(!showPassword)}
                          >
                            {showPassword ? (
                              <FontAwesomeIcon icon={faEye} />
                            ) : (
                              <FontAwesomeIcon icon={faEyeSlash} />
                            )}
                          </div>
                        </div>








                      <label htmlFor="email">Email</label>
                      <div className="flex-password">
                        <div className="icon-password">
                          <FontAwesomeIcon icon={faEnvelope} />
                        </div>

                        <div style={{ width: "100%" }} className="input-wrapper-2">
                          <input
                            style={{     margin: "0px 0px 4px 0px", width: "95%" }}
                            name="email"
                            id="email"
                            type="email"
                            className="form-control"
                            placeholder="Email"
                            
                            autoFocus
                                  value={formData.email}
                                  onChange={handleChange}
                                   
        
       
       
 
                          />
                           <span  className="underline-input-3"></span>
                   
                   
             
                        </div>
                      </div>

                      <div className={diaplay}>
                        <p style={{ margin: "0px" }}>
                          {sendEmail == "" ? "email requird" : ""}
                        </p>
                      </div>
                      <div className="submit-container" style={{ gap: "0px" ,    margin: "0px 0px 10px 0px"}}>
                       
                          <button
                               className={
                             
                                   "button-move"
                            }


                            disabled={false}
                            
                              style={{
                             
                                  width: "47%",
                                marginRight: "0",

                              }}
                            id="submit-loan-btn"
                            onClick={() => setRegister(false)}
                          >
                            الرجوع
                          </button>
                        

                         
                               




                       
                          {/* <button
                            className={
                              btnIsDisableSendEmail
                                ? "disabled submit"
                                : "submit gray back-btn"
                            }
                            // disabled={btnIsDisableSendEmail}
                            disabled
                            onClick={hangleSendEmail}
                          >
                            انشاء حساب
                          </button> */}

      

                      <button 
                      
                      type="submit"
                      
                              className={
                              disabledAdd
                                 ? "disabled  button-move "
                                  : "button-move"
                            }

                                 id="submit-loan-btn"
                                 disabled={disabledAdd}
                            
                              style={{
                                cursor: disabledAdd ? "not-allowed" : "",
                                  width: "47%",
                                marginRight: "7%",

                              }}
                      >  اضافه</button>

                     {/* {message && <p>{message}</p>} */}
  

                      </div>


             


                      <div className={diaplay}>
                        <p>{" go to emali" + " ' " + sendEmail + " ' "}</p>
                        <p>and Back enter password</p>
                        <span style={{ color: "green", fontWeight: "500" }}>
                          {getRandomNumber(1000, 9999)}
                        </span>
                      </div>
                    </form>
                          {showModal && errorMessage && (
        <div style={{ color: "red", marginTop: "10px" }}>{errorMessage}</div>
      )}
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>





















    )

  }else if (forgot === true) {
    return (
      <div style={{ marginTop: "0px" }} id="timeout" onClick={handelDivClick}> 
        <video
          width="300px"
          height="300px"
          className="vedio"
          autoplay="autoplay"
          muted
          loop
          src={video}
        >
          Your browser does not support the video tag.
        </video>
        <div className="video-overlay"></div>
        <div className="form-div ">
          <div className="form-login ">
            <div className="card css-login">
              <div className="ineer-container">
                <div className="form-logo">
                  <div className="form-logo-ineer ">
                    <img
                      src={loginlogo}
                      style={{ width: "70%" }}
                      alt="logo form"
                    />
                  </div>
                </div>
                <div div className="form-logo">
                  <div className="ineer-container">
                    <div className="css-text">
                      <label className="css-login-lable ">
                        رجاء تسجيل الايميل
                      </label>
                    </div>
                  </div>
                </div>

                <div className="form-logo">
                  <div className="input-form-login">
                    <form
                      className="flex-dir"
                      id="loan-form-start"
                      onSubmit={(event) => {
                        event.preventDefault();
                      }}
                    >
                      <div className="circle" style={{ zIndex: "-1" }}>
                        <FontAwesomeIcon icon={faUser} className="photo-user" />
                      </div>
                      <hr></hr>
                      <label htmlFor="email">Email</label>
                      <div className="flex-password">
                        <div className="icon-password">
                          <FontAwesomeIcon icon={faEnvelope} />
                        </div>

                        <div style={{ width: "100%" }} className="input-wrapper-2">
                          <input
                            style={{ margin: "6px", width: "95%" }}
                            name="email"
                            id="email"
                            type="email"
                            className="form-control"
                            placeholder="Email"
                            value={sendEmail}
                            autoFocus
                            onChange={(e) => setSendEmail(e.target.value)}
                          />
                           <span  className="underline-input-3"></span>
                   
                   
             
                        </div>
                      </div>

                      <div className={diaplay}>
                        <p style={{ margin: "0px" }}>
                          {sendEmail == "" ? "email requird" : ""}
                        </p>
                      </div>
                      <div className="submit-container">
                        <div>
                          <button
                            className="submit gray back-btn"
                            onClick={() => setForget(false)}
                          >
                            Back
                          </button>
                        </div>

                        <div>
                          <button
                            className={
                              btnIsDisableSendEmail
                                ? "disabled submit"
                                : "submit gray back-btn"
                            }
                            // disabled={btnIsDisableSendEmail}
                            disabled
                            onClick={hangleSendEmail}
                          >
                            Send
                          </button>
                        </div>
                      </div>
                      <div className={diaplay}>
                        <p>{" go to emali" + " ' " + sendEmail + " ' "}</p>
                        <p>and Back enter password</p>
                        <span style={{ color: "green", fontWeight: "500" }}>
                          {getRandomNumber(1000, 9999)}
                        </span>
                      </div>
                    </form>
                          {showModal && errorMessage && (
        <div style={{ color: "red", marginTop: "10px" }}>{errorMessage}</div>
      )}
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    );
  } else if(window.localStorage.getItem('name')){
  
  
  return(
    <div>
    <UseData.Provider value={true}>
      <Calculations passPhoto={photpUser} />
    </UseData.Provider>
  </div>
  )
  
  
}else if (check === false ) {

    return (
      <div style={{ marginTop: "0px" }} onClick={handelDivClick}>
            {/* <div style={{ marginTop: "0px" }} > */}
        <video
          width="300px"
          height="300px"
          className="vedio"
          autoplay="autoplay"
          muted
          loop
          src={video}
        >
          Your browser does not support the video tag.
        </video>
        <div className="video-overlay">
          <div className="form-div ">
            <div className="form-login ">
              <div className="card css-login">





          {showModal?(<>
          
            <>
    

      <MyVerticallyCenteredModal
        show={showModal}
        errorMassage={errorMessage}
        onHide={() => setShowModal(false)}
        dialogClassName="modal-90w"
      />
    </>
          
          </>):(<></>)}




                {/* <Modal2
                  style={{
                    width: "100px !important",
                    height: "100px !important",
                    background: "#ffafaf",
                  }}
                  isVisble={showModal}
                  errorMassage={errorMessage}
                /> */}


                <div className="ineer-container">
                  <div className="form-logo">
                    <div className="form-logo-ineer ">
                      <img
                        src={loginlogo}
                        style={{ width: "70%" }}
                        alt="logo form"
                      />
                    </div>
                  </div>
                  <div className="form-logo">
                    <div className="ineer-container">
                      <div className="css-text">
                        <label className="css-login-lable ">
                          رجاء تسجيل الدخول
                        </label>
                      </div>
                    </div>
                  </div>

                  <div className="form-logo">
                    <div className="input-form-login">
                      <form
                        className="flex-dir"
                        id="loan-form-start"
                        onSubmit={(event) => {
                          event.preventDefault();
                        }}
                      >
                        <div class="wrapper">
  <div class="loader">

                        <div className="circle" style={{ zIndex: "-1" }}>
                          {formInputs.inputName === "alaa" ? (
                            <img
                              alt="user-photo"
                              src={photoName}
                              className="user-name-input-css-log"
                            />
                          ) : (
                            <FontAwesomeIcon
                              icon={faUser}
                              className="photo-user-log"
                            />
                          )}
                        </div>
                        </div>
                        </div>
                        <hr style={{ margin:" 15px 5px 5px 5px" }}></hr>
                        <label htmlFor="name">Name</label>
                        <div className="flex-password">
                          <div className="icon-password">
                            <FontAwesomeIcon icon={faUser} />
                          </div>

                          <div style={{ width: "100%", margin: "6px" }} className="input-wrapper-2">
                            <input
                              style={{ margin: "0", height: "32px" }}
                              id="name"
                              className="form-control"
                              value={formInputs.inputName}
                              autoFocus
                              placeholder="اسم المستخدم"
                              onChange={(event) => {
                                setFormInputs({
                                  ...formInputs,
                                  inputName: event.target.value,
                                });
                              }}
                            />
                                                                                                    <span  className="underline-input-2"></span>
                   
                   
              
                          </div>
                        </div>

                        <label htmlFor="password">password</label>
                        <div className="flex-password">
                          <div className="icon-password">
                            <FontAwesomeIcon icon={faLock} />
                          </div>
                          <div style={{ width: "100%" }} className="input-wrapper-2">
                            <input
                              style={{
                                margin: "6px",
                                width: "95%",
                                height: "32px",
                              }}
                              className="form-control"
                              type={showPassword ? "text" : "password"}
                              autoComplete="true"
                              value={formInputs.inputPassword}
                              placeholder="الرقم السري"
                              id="password"
                              onChange={(e) =>
                                setFormInputs({
                                  ...formInputs,
                                  inputPassword: e.target.value,
                                })
                              }
                            />
                                                                                                    <span  className="underline-input-3"></span>
                   
                   
             
                          </div>

                          <div
                            className="icon-password-show"
                            onClick={() => setShowPassword(!showPassword)}
                          >
                            {showPassword ? (
                              <FontAwesomeIcon icon={faEye} />
                            ) : (
                              <FontAwesomeIcon icon={faEyeSlash} />
                            )}
                          </div>
                        </div>

                        <div className="forgot-password">
                          Forgot password ?
                          <span
                            onClick={() => {
                              setForget(true);
                            }}
                          >
                            Clik Here!
                          </span>
                        </div>



                        <div>
                          <div
                            style={{ display: "inline-block", width: "100%" }}
                          >
                            <button
                              className={
                                btnIsDisable
                                  ? "disabled  button-move "
                                  : "button-move"
                              }
                              disabled={btnIsDisable}
                              onClick={handelCheckDisable}
                              id="submit-loan-btn"
                              style={{
                                width: "47%",
                                marginRight: "0",
                                cursor: btnIsDisable ? "not-allowed" : "",
                              }}
                            >
                              الدخول
                              {/* <span className="btnRipples"></span> */}
                            </button>

  <button
                              className={
                              
                                  "button-move"
                              }
                             
                               onClick={() => {
                              setRegister(true);
                            }}
                              id="submit-loan-btn"
                              style={{
                                width: "47%",
                                marginRight: "6%",
                               
                              }}
                            >
                              التسجيل
                             
                            </button>









                          </div>
                        </div>
                      </form>
                            {showModal && errorMessage && (
        <div style={{ color: "red", marginTop: "10px" }}>{errorMessage}</div>
      )}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    );
  } else {
    return (
      <div>
        <UseData.Provider value={check}>
          <Calculations passPhoto={photpUser} />
        </UseData.Provider>
      </div>
    );
  }
}
export default App;
